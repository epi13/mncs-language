mncs 0.8;

module examples.invalid_vector_lane_mismatch;

fn invalid() -> (result: i32) {
    let left: vec<i32, 4> = vector(1, 2, 3, 4);
    let right: vec<i32, 2> = vector(5, 6);
    let result: vec<i32, 4> = vec_add_wrap(left, right);
    return extract_lane(result, 0);
}
