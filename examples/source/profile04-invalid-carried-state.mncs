mncs 0.4;
module examples.profile04.invalid_state;

fn invalid(input: u64) -> (result: u64) {
    iterate retries up_to 3 carrying state: u64 = input {
        next state = true;
    }
    return state;
}
